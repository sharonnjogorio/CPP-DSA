# Practical Exercise 1 — C++ Fundamentals

## Programs

| File | Description |
|------|-------------|
| `numberofdays.cpp` | Converts a number of days entered by the user into seconds |
| `volume.cpp` | Calculates the volume of a sphere given a radius using `pow()` |
| `areaandperimeter.cpp` | Computes the area and perimeter of a square using functions |
| `uppercase_lowercase.cpp` | Determines whether a character input is uppercase or lowercase |
| `pseudocode.cpp` | C++ implementation of a given pseudocode using a do-while loop |
| `arrayaverage.cpp` | Populates a 5-element array and calculates the average of its values |

## Codility Challenge

**Task:** Absolute Distinct Count — find the number of distinct absolute values in a sorted array.

**Language used:** JavaScript

**Result:** All example, correctness and performance tests passed — O(N) or O(N*log(N)) time complexity detected.

Screenshot of the analysis: `codilitytest.png`
